# Hardware Manifests

The frozen environment used for the completed Mac experiments is embedded in
the checked-in protocol JSON files. In particular, see:

- [`../dataset_and_split_hashes.json`](../dataset_and_split_hashes.json)
- [`../projected_lu_protocol.json`](../projected_lu_protocol.json)

Formal PINN and DeepONet CUDA hardware manifests do not exist yet because those
formal runs have not been completed.
