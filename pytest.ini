[pytest]
testpaths = tests

# The self-contained publication snapshot under method_extensions/tests is
# executed explicitly with its own PYTHONPATH. Restricting the default search
# avoids importing two files with the same test-module basename.
